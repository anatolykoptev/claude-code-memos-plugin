{
  "description": "MemOS memory hooks — inject relevant memories on prompt, save summaries before compaction",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node ${CLAUDE_PLUGIN_ROOT}/hooks/memos-inject.mjs",
            "timeout": 15
          }
        ]
      }
    ],
    "PreCompact": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node ${CLAUDE_PLUGIN_ROOT}/hooks/memos-precompact.mjs",
            "timeout": 120
          }
        ]
      }
    ]
  }
}
